# Excel转Word报告工具 - 详细说明文档

## 一、工具概述

本工具用于将Excel测试报告自动转换为标准Word文档格式。支持自动识别大用例、小用例结构，提取概述数据，生成格式化的Word报告。

---

## 二、核心概念

### 2.1 大用例 vs 小用例

```
大用例1（合并单元格，如：一般性项目）
├── 小用例1-1（如：外观检查）
├── 小用例1-2（如：尺寸测量）
└── 小用例1-3（如：重量测试）

大用例2（合并单元格，如：电气安全）
├── 小用例2-1（如：绝缘电阻）
└── 小用例2-2（如：耐压测试）
```

- **大用例**：通过**合并单元格**标识，代表一个测试分类
- **小用例**：大用例下方的具体测试项目，每行一个小用例

### 2.2 Excel结构要求

```
第1-4行：概述数据（键值对形式）
第5-7行：标题行（包含"试验项目"列）
第8行：大用例1（合并单元格）
第9-11行：小用例1-1、1-2、1-3
第12行：大用例2（合并单元格）
第13-14行：小用例2-1、2-2
...
```

---

## 三、大用例检测逻辑（核心）

### 3.1 检测流程

```
1. 找到"试验项目"列
   └── 在所有行中搜索包含"试验项目"的单元格
   └── 记录：test_col（列号）、header_row（行号）

2. 确定标题行合并范围
   └── 找到包含"试验项目"列的标题行合并单元格
   └── 记录：header_merge_end（标题行结束行号）

3. 检测大用例（三种方式，按优先级）
   
   方式1：检测试验项目列的行合并
   └── 条件：min_col == test_col 且 min_row > header_merge_end
   └── 适用于：大用例名字在"试验项目"列的情况
   
   方式2：检测试验项目前一列的行合并
   └── 条件：min_col == test_col - 1 且 min_row > header_merge_end
   └── 适用于：大用例名字在"试验项目"前一列的情况
   
   方式3：检测所有列合并（横向合并）
   └── 条件：min_col != max_col 且 min_row > header_merge_end
   └── 适用于：大用例名字横向合并多列的情况
```

### 3.2 合并单元格类型

| 类型 | 特征 | 示例 |
|------|------|------|
| **行合并** | min_row != max_row，min_col == max_col | 行8-10合并（纵向合并3行） |
| **列合并** | min_col != max_col，min_row == max_row | 列1-19合并（横向合并19列） |
| **区域合并** | min_row != max_row 且 min_col != max_col | 行8-10，列1-19合并 |

### 3.3 当前检测逻辑的问题

**问题**：代码只检测了三种情况，但可能遗漏其他情况。

**示例**：
- 找到：行100，列2-19的列合并
- 没找到：行8，列1-19的列合并

**可能原因**：
1. `header_merge_end` 的值大于等于8，导致行8被过滤
2. 行8的合并单元格值可能为空
3. 行8的合并单元格可能不符合检测条件

---

## 四、小用例解析逻辑

### 4.1 解析流程

```
对于每个大用例 (start_row, end_row, big_case_name):
    
    1. 确定小用例搜索范围
       └── 起始：end_row + 1
       └── 结束：下一个大用例的 start_row - 1（或文件末尾）
    
    2. 遍历搜索范围内的每一行
       └── 读取"试验项目"列的值
       └── 如果值不为空，则是一个小用例
       └── 提取小用例数据（样机数量、样机编号等）
```

### 4.2 小用例数据提取

通过 `extract_testcase_data(row_data)` 函数提取：

| 字段 | 可能的Excel列名 |
|------|----------------|
| 开始日期 | 开始日期、开始时间、起始日期 |
| 结束日期 | 结束日期、结束时间、终止日期 |
| 样机数量 | 样机数量、数量、样品数量、DV数量 |
| 样机编号 | 样机编号、编号、样品编号、机号 |
| 试验机构 | 试验机构、检测机构、测试机构 |
| 试验环境 | 试验环境、环境条件、测试环境 |
| 试验标准 | 试验标准、标准、测试标准 |
| 试验条件 | 试验条件、条件、试验方法 |
| 规格要求 | 规格要求、要求、技术要求 |
| 试验数据 | 试验数据、数据、测试数据 |
| 试验结论 | 试验结论、结论、测试结论 |

---

## 五、配置文件说明

配置文件路径：`D:\AI\report_config.txt`

### 5.1 主要配置项

```ini
# Excel文件路径
excel_file=D:\AI\test_data.xlsx

# 输出目录（None表示与Excel同目录）
output_dir=None

# 要处理的sheet
# None = 全部sheet
# 数字 = 指定索引（0表示第一个）
# 字符串 = 指定名称（如"一般性项目"）
sheets=None

# 合并模式
# True = 多个sheet合并到一个Word
# False = 每个sheet生成单独Word
merge=False
```

### 5.2 小用例属性配置

配置值优先级：**配置文件 > Excel数据**

```ini
# 样机数量（对所有小用例生效）
样机数量=3 pcs

# 样机编号（自动格式化为 1#、2#、3#）
样机编号=1/2/3

# 试验机构
试验机构=xxx检测中心

# 试验环境
试验环境=常温常湿

# 试验标准
试验标准=GB/T 12345-2020

# 试验条件
试验条件=额定负载

# 规格要求（支持多行，用\n表示换行）
规格要求=温升≤80K\n绝缘电阻≥100MΩ
```

---

## 六、调试信息说明

运行脚本时会打印详细的调试信息：

### 6.1 合并单元格检测

```
=== 调试信息：所有合并单元格 ===
  合并范围: 行1-3, 列1-1, 值: xxx
  合并范围: 行5-7, 列1-19, 值: 标题行
  合并范围: 行8-8, 列1-19, 值: 一般性项目
  合并范围: 行100-100, 列2-19, 值: 电气安全
=== 合并单元格总数: 4 ===
```

### 6.2 大用例检测

```
找到'试验项目'列: 第5行, 第3列
标题行合并范围: 第5-7行
header_merge_end = 7 (大用例必须在此行之后)

检测到列合并: 行8, 列1-19, 值: 一般性项目
  -> 添加为大用例
检测到列合并: 行100, 列2-19, 值: 电气安全
  -> 添加为大用例

=== 找到的大用例列表 ===
  [1] 行8-8: 一般性项目
  [2] 行100-100: 电气安全
=== 共 2 个大用例 ===
```

### 6.3 小用例解析

```
解析大用例 '一般性项目':
  合并行范围: 8-8
  小用例搜索范围: 9 到 99
  试验项目列索引: 3
  第9行, 试验项目列值: 外观检查
    -> 找到小用例: 外观检查
  第10行, 试验项目列值: 尺寸测量
    -> 找到小用例: 尺寸测量
```

---

## 七、常见问题排查

### 7.1 大用例检测不正确

**排查步骤**：

1. 查看调试信息中的"所有合并单元格"列表
2. 确认大用例的合并单元格是否在列表中
3. 检查 `header_merge_end` 的值
4. 确认大用例合并单元格的值是否为空

**可能原因**：
- 大用例行号 <= header_merge_end（被过滤）
- 大用例合并单元格值为空
- 大用例合并类型不符合检测条件

### 7.2 小用例检测不正确

**排查步骤**：

1. 查看大用例的"小用例搜索范围"是否正确
2. 检查每一行"试验项目列值"是否正确
3. 确认试验项目列索引是否正确

**可能原因**：
- 小用例不在搜索范围内
- 试验项目列的值为空
- 试验项目列索引错误

### 7.3 指定sheet不生效

**已修复**：确保使用最新代码（commit `024c45d` 之后）

**问题原因**：`find_merged_cells_info` 使用 `wb.active` 而非用户指定的sheet

---

## 八、代码文件结构

```
D:\AI\excel_to_word_report.py
├── 字段映射配置
│   ├── OVERVIEW_FIELD_MAPPING（概述字段）
│   └── TESTCASE_FIELD_MAPPING（小用例字段）
├── 工具函数
│   ├── clean_case_number()（清理用例名数字前缀）
│   ├── extract_case_number()（提取用例数字）
│   ├── format_sample_number()（格式化样机编号）
│   └── smart_match_field()（智能字段匹配）
├── ExcelToWordReport 类
│   ├── load_excel()（加载Excel）
│   ├── find_merged_cells_info()（检测大用例）★核心
│   ├── parse_test_cases()（解析大用例和小用例）★核心
│   ├── build_column_mapping()（建立列名映射）
│   ├── extract_testcase_data()（提取小用例数据）
│   └── generate_word_report()（生成Word报告）
└── 主函数
    ├── process_sheets()（处理多个sheet）
    └── load_config()（加载配置文件）
```

---

## 九、版本历史

| 提交 | 说明 |
|------|------|
| `024c45d` | fix: 修复指定sheet后仍读取活动sheet的问题 |
| `6ada51e` | fix: 大用例检测增加前一列合并检测 |
| `76cef85` | fix: 检测所有列合并作为大用例 |
| `abf033b` | debug: 打印header_merge_end值 |

---

## 十、待解决问题

1. **大用例检测逻辑需要优化**
   - 当前只检测三种情况，可能遗漏其他合并方式
   - 需要用户反馈具体的调试信息来定位问题

2. **建议改进方案**
   - 方案A：检测所有行号 > header_merge_end 的合并单元格
   - 方案B：让用户在配置文件中指定大用例的列号
   - 方案C：通过其他特征识别大用例（如特定的单元格样式）
